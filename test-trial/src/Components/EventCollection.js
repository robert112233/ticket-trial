// import ItemCard from "./ItemCard";

// export default function EventCollection({ items }) {
//   console.log({ items }, "in event collection");

//   return (
//     <main>
//       {items.map((item) => {
//         return <ItemCard key={item.name} {...item} />;
//       })}
//     </main>
//   );
// }
